# Required for Grok provider (default)
XAI_API_KEY=xai-...

# Required for Anthropic provider (--provider anthropic)
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: Default model (defaults per provider)
# FRIDAY_MODEL=grok-4-1-fast-reasoning-latest
